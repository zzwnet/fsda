let 声明变量
1、变量不能重复声明
2、块级作用域 {}中有效，例如for if while else等
3、不存在变量提升，不允许在变量声明前去使用该变量
4、不影响作用域链，上级声明的函数，下级可以用

const 声明常量
1、一定要赋初始值
2、一般常量使用大写（潜规则）
3、常量的值不能被修改
4、块级作用域 作用域以外不能被访问
5、对于数组和对象的元素修改，不算做对常量的修改，不会报错

结构赋值
 